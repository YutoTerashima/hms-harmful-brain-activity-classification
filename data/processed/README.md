# Processed Data Artifacts

This directory is reserved for small, committed metadata artifacts only.

The original Kaggle parquet data and downloaded competition files must stay in `data/raw/`, which is
ignored by Git. Run `scripts/download_data.py` and `scripts/prepare_metadata.py` after accepting the
competition rules on Kaggle.

